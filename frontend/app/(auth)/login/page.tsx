// ============================================================
// ROUTE: /login — Admin login page
// ============================================================

import LoginView from "@/views/LoginView";

export default function LoginPage() {
  return <LoginView />;
}
